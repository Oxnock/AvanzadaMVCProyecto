@model IEnumerable<ProyectoIdentity.Models.Asistencia>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Semana)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.vAsistencia)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Semana)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.vAsistencia)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AsistenciaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AsistenciaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AsistenciaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
