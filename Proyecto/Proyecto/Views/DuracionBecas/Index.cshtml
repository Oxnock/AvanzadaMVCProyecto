@model IEnumerable<Proyecto.Models.DuracionBeca>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.FechaInicial)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.FechaFinal)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaInicial)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaFinal)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DuracionBecaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DuracionBecaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DuracionBecaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
