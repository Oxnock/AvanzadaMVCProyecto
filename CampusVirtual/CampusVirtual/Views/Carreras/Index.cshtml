@model CampusVirtual.ViewModels.CarrerasViewModels.IndexViewModel
@{
	ViewBag.Title = "Index";
	Layout = "~/Views/Shared/_Layout.cshtml";
}
@inject SignInManager<Usuario> SignInManager


@*@if (User.IsInRole("Administrador"))
{*@
	<h2 class="title">Administrar Cursos</h2>
	<a asp-action="Crear" asp-controller="Curso"  class="btn btn-primary btn-md btn-submit">Crear carrera</a>
}

@*@if (User.IsInRole("Profesor"))
{*@
	<h2 class="title">Carreras</h2>

	<table>
		<thead>
			<tr>
				<th>Curso</th>
				@*@if (User.IsInRole("Administrador"))
				{*@
					<th>Opciones</th>
				@*}*@
			</tr>
		</thead>
		<tbody>
			@foreach (var curso in Model.Carrera)
			{
			<tr>
				<td>
					@curso.Nombre
				</td>
				<td>
					@curso.Descripcion
				</td>
				<td>
					@curso.Director
				</td>
				@*@if (User.IsInRole("Administrador"))
	{*@
			<td>
				<a asp-action="Editar" asp-controller="Curso" asp-route-id="@curso.CarrerasId">Editar</a>
				<a asp-action="Eliminar" asp-controller="Curso" asp-route-id="@curso.CarrerasId">Eliminar</a>
			</td>
@*}*@
			</tr>
			}
		</tbody>
	</table>
@*}*@

@*@if (User.IsInRole("Estudiante"))
{
	<h2 class="title">Carreras</h2>

	<table>
		<thead>
			<tr>
				<th>Curso</th>
				
			</tr>
		</thead>
		<tbody>
			@foreach (var curso in Model.Carrera)
			{
			<tr>
				<td>
					@curso.Nombre
				</td>
				<td>
					@curso.Descripcion
				</td>
				<td>
					@curso.Director
				</td>

			</tr>
			}
		</tbody>
	</table>*@
}


